{
  "name": "liquidate",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run prettier && bun run build",
    "build": "tsup index.ts --format esm --dts",
    "prettier": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@permaweb/aoconnect": "^0.0.85",
    "ao-tokens": "^0.0.6",
    "ar-gql": "^3.1.1",
    "arbundles": "^0.11.2",
    "liquidops": "^1.0.75",
    "prettier": "^3.5.3",
    "tsup": "^8.5.0",
    "warp-arbundles": "^1.0.4"
  }
}
